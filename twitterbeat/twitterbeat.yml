twitterbeat:  
  twitter:
    names: ${SCREEN_NAMES:["@realDonaldTrump", "@ziwe"]}
    consumer_key: ${CONSUMER_KEY}
    consumer_secret: ${CONSUMER_SECRET}
    access_key: ${ACCESS_KEY}
    access_secret: ${ACCESS_SECRET}

output:
  elasticsearch:    
    hosts: ["localhost:9200"]
    protocol: "http"